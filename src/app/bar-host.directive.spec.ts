import { BarHostDirective } from './bar-host.directive';

describe('BarHostDirective', () => {
  it('should create an instance', () => {
    const directive = new BarHostDirective();
    expect(directive).toBeTruthy();
  });
});
